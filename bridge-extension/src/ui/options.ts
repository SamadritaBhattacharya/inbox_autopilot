/**
 * The options page is the popup's content without the popup's lifetime.
 *
 * Chrome opens this from the extensions list, where a user who has lost the popup goes
 * looking. It re-uses the same storage keys, so pairing in either place pairs both.
 */
import "./popup";
